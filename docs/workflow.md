# Workflow

- One backlog item per branch.
- For each item: implement minimal → tests → run tests/evals → docs update → commit.
- If blocked/ambiguous: STOP and ask the user.
- Do not add extra UX; Discord only.
